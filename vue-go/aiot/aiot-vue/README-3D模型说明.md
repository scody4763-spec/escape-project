# 火灾逃生系统 - 3D建筑模型说明

## 项目概述

基于 Three.js 的 3D 建筑逃生模拟系统，集成 Greedy、ACO、IACO 路径规划、动态逃生演示、火焰/烟雾特效等功能。支持 6 层建筑模型，4 个楼梯、4 个出口，可交互式模拟火灾逃生场景。

---

## 模型结构

### 建筑规格

| 参数 | 值 |
|------|-----|
| 楼层数 | 6 层（1F-6F） |
| 层高 | 4.5 米 |
| 建筑总高度 | 27 米 |
| 楼梯数量 | 4 个（东/西/北/南） |
| 出口数量 | 4 个（东/西/南/北） |
| 电梯数量 | 1 个（火灾模式禁用） |

### 每层布局

| 节点类型 | 数量 | 说明 |
|---------|------|------|
| 房间（room） | 3 个 | 办公室/会议室/机房等 |
| 走廊（corridor） | 3 个 | 主走廊 + 东西走廊 |
| 楼梯（stair） | 4 个 | 东/西/北/南楼梯 |
| 电梯（elevator） | 1 个 | 火灾时禁用 |

### 楼层功能分布

| 楼层 | 功能 |
|------|------|
| 1F | 大厅 + 商铺（4间） |
| 2F | 办公室 + 会议室 |
| 3F | 展厅 + 机房 + 休息区 |
| 4F | 办公室 + 培训室 |
| 5F | 实验室 + 图书馆 + 会议室 |
| 6F | 机房 + 休息区 + 大厅 |
### 楼层节点分布表（后端楼层模型）

| 楼层 | Y 坐标 | 房间/功能区 | 走廊 | 楼梯 | 电梯 | 出口 |
|------|--------|-------------|------|------|------|------|
| 1F | 0 | f1-lobby 大厅；f1-shop-a/b/c/d 商铺×4 | f1-cor-w 西走廊、f1-cor-e 东走廊、f1-cor-n 北走廊、f1-cor-s 南走廊 | f1-stair-east 东楼梯、f1-stair-west 西楼梯、f1-stair-north 北楼梯、f1-stair-south 南楼梯 | f1-elevator | f1-exit-east 东出口、f1-exit-west 西出口、f1-exit-south 南出口、f1-exit-north 北出口 |
| 2F | 4.5 | f2-office-a 办公室A、f2-office-b 办公室B、f2-meeting 会议室 | f2-cor-main 主走廊、f2-cor-w 西走廊、f2-cor-e 东走廊 | f2-stair-east/west/north/south 楼梯×4 | f2-elevator | — |
| 3F | 9 | f3-hall 展厅、f3-server 机房、f3-lounge 休息区 | f3-cor-main 主走廊 | f3-stair-east/west/north/south 楼梯×4 | f3-elevator | — |
| 4F | 13.5 | f4-office-a 办公室A、f4-office-b 办公室B、f4-training 培训室 | f4-cor-main 主走廊、f4-cor-w 西走廊、f4-cor-e 东走廊 | f4-stair-east/west/north/south 楼梯×4 | f4-elevator | — |
| 5F | 18 | f5-lab 实验室、f5-library 图书馆、f5-meeting-b 会议室B | f5-cor-main 主走廊、f5-cor-w 西走廊、f5-cor-e 东走廊 | f5-stair-east/west/north/south 楼梯×4 | f5-elevator | — |
| 6F | 22.5 | f6-server 机房、f6-lounge 休息区、f6-hall 大厅 | f6-cor-main 主走廊、f6-cor-w 西走廊、f6-cor-e 东走廊 | f6-stair-east/west/north/south 楼梯×4 | f6-elevator | — |

---

## 3D 模型样式

### 建筑外观

- **楼板**：深灰色平面（`#2a2f3a`）
- **墙壁**：白色半透明（`#d8dee9`），带边线
- **门**：蓝色半开状态（`#88c0d0`）
- **窗户**：浅蓝色玻璃（`#7dd3fc`）
- **外墙**：灰色半透明（`#94a3b8`），带窗户开洞
- **屋顶**：深灰色（`#475569`）

### 室内设施

| 设施 | 样式 | 位置 |
|------|------|------|
| 楼梯 | 黄色台阶 + 扶手 | 4 个楼梯间 |
| 电梯 | 紫色半透明箱体 + 双开门 | 每层1个 |
| 消防栓 | 红色圆柱 + 接口管 | 1F |
| 监控室 | 蓝色地板 + 玻璃墙 + 显示屏 | 1F |
| 配电间 | 黄色警示地板 + 电箱 | 1F |

### 家具布局

根据房间类型自动布置：

| 房间类型 | 家具 |
|---------|------|
| 商铺 | 展示柜 + 收银台 |
| 办公室 | L型桌 + 显示器 + 文件柜 + 椅子 |
| 会议室 | 椭圆桌 + 围椅 |
| 机房 | 服务器机柜排列 |
| 休息区 | L型沙发 + 茶几 |

### 吸顶灯

- **数量**：每层 6 个（2x3 网格分布）
- **样式**：圆形薄盘（`#fff7ed` 暖白色）
- **光源**：PointLight 向下发光
- **火灾时**：起火点附近变为橙红色警告

---

## 指示灯系统

### 布局

每层 12 个指示灯，分布在：

| 位置 | 数量 | 说明 |
|------|------|------|
| 北墙内侧 | 2 | 贴墙安装 |
| 南墙内侧 | 2 | 贴墙安装 |
| 西墙内侧 | 2 | 贴墙安装 |
| 东墙内侧 | 2 | 贴墙安装 |
| 东楼梯口 | 1 | 指向楼梯 |
| 西楼梯口 | 1 | 指向楼梯 |
| 北楼梯口 | 1 | 指向楼梯 |
| 南楼梯口 | 1 | 指向楼梯 |

### 状态

| 状态 | 颜色 | 说明 |
|------|------|------|
| 默认 | 绿色矩形 | 正常状态 |
| 逃生路径上 | 绿色发光箭头 | 指向逃生方向 |
| 起火点附近 | 红色发光 | 警告 |
| 模拟结束 | 恢复绿色矩形 | 默认状态 |

---

## 功能模块

### 1. 3D 视图

- **鼠标操作**：左键旋转、右键平移、滚轮缩放
- **键盘操作**：WASD/方向键移动相机
- **视角预设**：透视、俯视、正视、侧视、适配
- **相机面板**：实时显示和调整相机位置/目标

### 2. 2D 平面图

- **楼层切换**：1F-6F 标签页切换
- **节点显示**：不同颜色区分节点类型
- **路径叠加**：显示计算出的逃生路径
- **图例**：房间、走廊、出口、楼梯、电梯等

### 3. 逃生路径计算

- **算法选择**：Greedy、基础 ACO、改进 ACO（IACO）和 compare
- **起点选择**：下拉框选择或点击 3D 模型放置
- **火灾模式**：开启后电梯禁用，路径避开电梯
- **起火点选择**：选择节点作为火灾起源

### 4. 逃生模拟

- **小人模型**：蓝色衣服 + 黄色头部，放大 2 倍
- **行走动画**：腿部/手臂摆动
- **移动方式**：沿计算出的逃生路径平滑移动
- **速度控制**：0.5x ~ 3x 可调

### 5. 火焰/烟雾效果

- **火焰粒子**：橙红色，向上跳动 + 闪烁
- **烟雾粒子**：灰色，缓慢上升扩散
- **火焰光源**：橙红色 PointLight 闪烁
- **影响范围**：起火点及相邻节点

### 6. 楼层高亮

- **模拟时**：小人所在楼层高亮，其他楼层半透明
- **跨楼层**：小人移动时自动切换高亮楼层
- **模拟结束**：所有楼层恢复正常显示

---

## 使用方法

### 启动服务

```bash
# 1. 启动后端容器
cd ~/escape-project
docker compose up -d

# 2. 启动算法服务（已包含在 compose 中）
docker compose up -d algorithm-service

# 3. 用 tmux 启动前端（断线不丢失）
tmux new -d -s frontend "cd ~/escape-project/aiot/aiot-vue && npm run dev -- --host 0.0.0.0 --port 5173"
```

### 访问系统

浏览器打开 `http://服务器IP:5173`，登录后进入 **楼层模型** 页面。

### 逃生模拟流程

1. **选择起点**：从下拉框选择，或点击"📍 点击放置小人"在 3D 模型上点击放置
2. **设置起火点**（可选）：在模拟工具栏选择起火节点
3. **开启火灾模式**（可选）：电梯禁用，路径避开电梯
4. **计算路径**：点击"🔥 计算逃生路径"
5. **开始模拟**：点击"▶ 开始"
6. **观察效果**：
   - 小人沿路径行走
   - 路径上指示灯变绿色箭头
   - 起火点冒火焰 + 烟雾
   - 起火点附近吸顶灯变橙红色
   - 楼层自动切换高亮

### 相机操作

| 操作 | 说明 |
|------|------|
| 左键拖拽 | 旋转视角 |
| 右键拖拽 | 平移 |
| 滚轮 | 缩放 |
| WASD / 方向键 | 移动相机 |
| Q / 空格 | 上升 |
| E / Shift | 下降 |

### 语言切换

右上角 "中文 / English" 切换界面语言。

---

## 技术栈

| 技术 | 用途 |
|------|------|
| Vue 3 + Vite | 前端框架 |
| Three.js | 3D 渲染 |
| Element Plus | UI 组件库 |
| GoFrame | 后端框架 |
| Python + FastAPI | 算法服务 |
| Greedy / ACO / IACO | 路径规划 |
| MySQL | 数据存储 |
| Docker | 容器化部署 |

---

## 文件结构

```
aiot-vue/src/
├── views/
│   └── FloorModel.vue      # 楼层模型主页面（3D/2D视图、模拟控制）
├── utils/
│   ├── mallBuilder.js       # 3D建筑生成器（墙壁、家具、指示灯、吸顶灯）
│   └── pathRenderer.js      # 逃生路径渲染（管道、箭头、起终点标记）
├── api/
│   └── escape.js            # API接口（节点、边、路径计算）
├── locales/
│   └── index.js             # 多语言包（中文/英文）
└── stores/
    └── settings.js          # 设置存储（语言、主题）
```

---

## 数据库结构

```sql
-- 节点表
graph_node (id, floor_id, node_key, name, node_type, x, y, z, is_exit, status)

-- 边表
graph_edge (id, from_node_key, to_node_key, distance, base_weight, width, bidirectional, status)

-- 传感器绑定表
sensor_node_binding (id, sensor_id, node_key, influence_radius)

-- 算法配置表
algorithm_config (id, algorithm, name, params_json, is_default)
```

---

## 常见问题

### Q: 前端显示"获取数据失败"
A: 检查后端容器是否运行：`docker ps`，确认 MySQL 密码正确。

### Q: 计算逃生路径失败
A: 确认算法服务运行：`curl http://localhost:8090/health`，返回 `{"status":"ok"}`。

### Q: SSH 断开后前端停止
A: 使用 tmux 启动：`tmux new -d -s frontend "npm run dev -- --host 0.0.0.0 --port 5173"`

### Q: 服务器重启后服务丢失
A: 重新执行 `docker compose up -d` 并重启 tmux 前端会话，或配置 systemd 自动启动。
